<%= form_for("room", :url => '/rooms', remote: true) do |f| %> 
  <h2>New Room Name</h2>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :house_id %>
  <%= f.select :house_id, @house_options %>
  <%= f.submit "Create Room", class: "button" %>
<% end %> 